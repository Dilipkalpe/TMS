# Demo RMS — frontend (demo-rms.company.com)
upstream demo_rms_web { server 127.0.0.1:8203; keepalive 16; }
server {
    listen 80;
    server_name demo-rms.company.com;
    return 301 https://$host$request_uri;
}
server {
    listen 443 ssl http2;
    server_name demo-rms.company.com;
    ssl_certificate     /etc/letsencrypt/live/demo-rms.company.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/demo-rms.company.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    client_max_body_size 25M;
    location / {
        proxy_pass http://demo_rms_web;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
