# Production RMS — frontend (rms.company.com)
upstream prod_rms_web { server 127.0.0.1:8103; keepalive 16; }
server {
    listen 80;
    server_name rms.company.com;
    return 301 https://$host$request_uri;
}
server {
    listen 443 ssl http2;
    server_name rms.company.com;
    ssl_certificate     /etc/letsencrypt/live/rms.company.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/rms.company.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    client_max_body_size 25M;
    location / {
        proxy_pass http://prod_rms_web;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
