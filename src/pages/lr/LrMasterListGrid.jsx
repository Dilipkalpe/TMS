import LrListPageContent from '../../components/lr/LrListPageContent'

/** Master LR list tab — mockup layout with KPIs, filters, and status badges. */
export default function LrMasterListGrid({ onChanged }) {
  return <LrListPageContent embedded onChanged={onChanged} />
}
